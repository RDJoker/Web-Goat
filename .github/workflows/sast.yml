name: Call SAST Pipeline

on:
  push:
    branches:
      - main
  pull_request:

permissions:
      actions: read
      contents: read
      security-events: write 

jobs:
  call-reusable:
    uses: RDJoker/Security-Pipeline/.github/workflows/sast.yml@main
    
