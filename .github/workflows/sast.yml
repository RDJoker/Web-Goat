name: Call SAST Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  call-reusable:
    uses: RDJoker/sast/.github/workflows/sast.yml@main
    permissions:
      actions: read
      contents: read
      security-events: write
